# Workspace Instructions & Memory

## Build & Git Workflow Rules
- **Do NOT run build command** (`npm run build` / `vite build`) automatically or without explicit user instruction.
- **Do NOT execute `git push`** under any circumstances during automated build or script steps.
- Maintain all changes locally in memory / local repository.
