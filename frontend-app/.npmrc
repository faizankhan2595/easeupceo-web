legacy-peer-deps=true
# pnpm skips pre/post script hooks by default (npm ran them). Enable so
# `pnpm run deploy` still runs the `predeploy` (build) step first.
# Use `pnpm run deploy` — NOT `pnpm deploy` (reserved pnpm built-in).
enable-pre-post-scripts=true